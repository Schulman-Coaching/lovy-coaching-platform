import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Lovy Coaching | Relationship Coach Certification",
  description: "Become a certified relationship coach with our Torah-based training program. Learn from Rabbi Shlomo and Malky Lovy's proven methodology to help couples build stronger marriages.",
  keywords: "relationship coaching, marriage coaching, Torah coaching, Jewish coaching certification, couples coaching training",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
